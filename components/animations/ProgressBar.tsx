"use client";

/**
 * Progress Bar
 *
 * Goal: Animate a bar filling up smoothly
 *
 * Hints:
 * - Animate width (%) or scaleX with a transition
 * - Use ease-out for a natural deceleration feel
 * - Try adding a shimmer/glow effect with @keyframes
 * - Consider what happens at 100% — a color change or pulse?
 */
export default function ProgressBar() {
  return <div></div>;
}
